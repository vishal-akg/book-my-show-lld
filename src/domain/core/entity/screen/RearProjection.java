package domain.core.entity.screen;

import domain.core.entity.theatre.Theatre;

public class RearProjection extends Screen{
    public RearProjection(int id, SeatConfiguration seatConfiguration) {
        super(id, seatConfiguration);
    }
}
