package domain.core.entity.screen;

import domain.core.entity.theatre.Theatre;

public class FrontProjection extends Screen{
    public FrontProjection(int id, SeatConfiguration seatConfiguration) {
        super(id, seatConfiguration);
    }
}
